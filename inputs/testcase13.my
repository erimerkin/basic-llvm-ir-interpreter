# Let's do some cool math!

acoolvariable = notsocoolvariable * notsocoolvariable
acoolervariable = 10 * acoolvariable

# a variable so cool that it cannot be computed...
toocool variable = ((0 + acoolvariable) + 5) + (acoolvariable * 10))  # Syntax error 
